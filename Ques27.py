# Program to convert a string into a list of its characters

# Taking a string as input from the user
user_string = input("Enter a string: ")

# Converting the string into a list of its characters
char_list = list(user_string)

# Printing the list of characters
print(f"The list of characters is: {char_list}")
